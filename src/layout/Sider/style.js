import styled from 'styled-components'
import global from 'styles/globalStyle'

export const Container = styled.div`
	width: ${global.siderWidth};
	height: 100%;
	border-right: ${global.normalBorder};
	position: fixed;
	top: 0;
`